/**
 * Soft, green-tinted blurhash shown while a card image loads. Filling the
 * frame with this avoids the empty tinted box (which reads as an outline)
 * appearing before the real image fades in.
 */
export const CARD_IMAGE_BLURHASH = 'L6Pj0^i_.AyE_3t7t7R**0o#DgR4';
